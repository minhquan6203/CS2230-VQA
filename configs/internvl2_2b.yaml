system_prompt: "Dựa trên hình ảnh được cung cấp, hãy trả lời bằng tiếng Việt. Câu trả lời cần ngắn gọn, trực tiếp vào vấn đề mà không cần sử dụng chủ ngữ. Chỉ sử dụng các OCR xuất hiện trong hình ảnh cho câu trả lời."

model:
  name: "OpenGVLab/InternVL2-2B"
  type: "internvl2"
  device: "cuda:0"  # chỉ định GPU: "cuda:0", "cuda:1", ... | null = để HF tự phân bổ
  max_num_tiles: 4   # mỗi tile = 256 tokens, max 5 tiles (4+thumbnail) = 1280 image tokens

data:
  train_file: "./data/ViTextVQA_train.json"
  dev_file: "./data/ViTextVQA_dev.json"
  test_file: "./data/ViTextVQA_test_gt.json"
  image_dir: "./data/st_images"

training:
  output_dir: "checkpoints/internvl2_2b"
  num_train_epochs: 3
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 16   # effective batch = 16
  learning_rate: 2.0e-4
  weight_decay: 0.01
  warmup_steps: 200
  max_length: 2048
  logging_steps: 50
  eval_steps: 500
  save_steps: 500
  save_total_limit: 2
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  greater_is_better: false
  dataloader_num_workers: 2
  report_to: "wandb"
  seed: 42

lora:
  r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  target_modules: ["wqkv", "wo", "w1", "w2", "w3"]
  bias: "none"

quantization:
  load_in_4bit: true
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_compute_dtype: "bfloat16"
  bnb_4bit_use_double_quant: true

wandb:
  project: "CS2230-VQA"
  run_name: "internvl2_2b"

evaluation:
  checkpoint: null
  batch_size: 4
  max_new_tokens: 64
  num_beams: 1
  results_file: "results/internvl2_2b_test.json"
